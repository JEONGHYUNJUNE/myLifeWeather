import {describe,expect,it} from 'vitest';import {analyzeWeather,classifyWeather,validatePeriods} from './weather';import type {DailyWeather} from './types';
describe('날씨 분류',()=>{it('눈과 비의 우선순위를 적용한다',()=>{expect(classifyWeather({date:'2020-01-01',snowfall_sum:.1,rain_sum:4})).toBe('snowy');expect(classifyWeather({date:'2020-01-02',rain_sum:.1,cloud_cover_mean:0})).toBe('rainy')});it('일조와 구름량을 분류한다',()=>{expect(classifyWeather({date:'x',sunshine_duration:70,daylight_duration:100})).toBe('sunny');expect(classifyWeather({date:'x',cloud_cover_mean:80})).toBe('cloudy');expect(classifyWeather({date:'x'})).toBe('unknown')})});
describe('생활 기간',()=>{it('겹침과 빈 구간을 구분한다',()=>{expect(validatePeriods([{startDate:'2020-01-01',endDate:'2020-01-10',isCurrent:false},{startDate:'2020-01-10',endDate:'2020-01-20',isCurrent:false}]).overlaps).toHaveLength(1);expect(validatePeriods([{startDate:'2020-01-01',endDate:'2020-01-10',isCurrent:false},{startDate:'2020-01-12',endDate:'2020-01-20',isCurrent:false}]).gaps).toHaveLength(1)})});
describe('집계',()=>{const days:DailyWeather[]=[{date:'2020-02-29',city:'서울',placeType:'activity',temperature_2m_mean:10,temperature_2m_max:15,temperature_2m_min:4,cloud_cover_mean:10},{date:'2021-02-28',city:'부산',placeType:'home',temperature_2m_mean:12,temperature_2m_max:18,temperature_2m_min:7,rain_sum:2}];it('날짜를 한 번씩 집계하고 지역을 병합한다',()=>{const r=analyzeWeather(days,'2000-02-29');expect(r.totalDays).toBe(2);expect(r.cities).toHaveLength(2);expect(r.awayPercent).toBe(50);expect(Object.values(r.counts).reduce((a,b)=>a+b,0)).toBe(2)});it('2월 29일 생일은 윤년 날짜만 센다',()=>expect(analyzeWeather(days,'2000-02-29').birthdays.total).toBe(1))});
