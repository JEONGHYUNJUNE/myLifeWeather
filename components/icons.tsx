export function WeatherGlyph({type='sunny',size=28}:{type?:string,size?:number}){if(type==='rainy')return <span style={{fontSize:size}}>☂</span>;if(type==='snowy')return <span style={{fontSize:size}}>❄</span>;if(type==='cloudy')return <span style={{fontSize:size}}>☁</span>;if(type==='partly_cloudy')return <span style={{fontSize:size}}>◒</span>;return <span style={{fontSize:size}}>☀</span>}
